#!/usr/bin/env node
var canadian = require('../index');
var argument = process.argv[2];

switch (true) {
  case (argument !== ''):
    canadian(argument);
    break;
  default:
    canadian();
    break;
}